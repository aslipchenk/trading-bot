export enum StatusEnum {
  inProgress = 'inProgress',
  finished = 'finished',
}

export enum OutboxStatusesEnum {
  filled = 'filled',
  failed = 'failed',
  broken = 'broken',
}

export enum IndicatorsEnum {
  RSI = 'RSI',
}

export enum SidesEnum {
  LONG = 'LONG',
  SHORT = 'SHORT',
}
